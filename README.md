# Trinnov homepage concept

Prototype Web responsive et non officiel inspiré de la page d’accueil de Trinnov.

## Lancer en local

```bash
pnpm install
pnpm dev -- --port 3000
```

Ouvrir ensuite `http://localhost:3000`.

## Vérifier la production

```bash
pnpm build
```
